package com.surelogic.jsure.client.eclipse.model.selection;

import com.surelogic.dropsea.IPromiseDrop;

/**
 * Marker that a {@link Filter} implementation only allows JSure promises
 * through it, instances of {@link IPromiseDrop}. This helps the UI show filter
 * choices after this filter.
 */
public interface IOnlyPromisesPorus {
  // marker
}
