# $Header$

/** Note that the class to which this anno applies is "part of" the
 * named module.  For the moment, we'll restrict this anno to appear
 * only in package-info.java or package.xml files.
 */
 
//     $modifiers:Modifiers  
public operator Module implements edu.cmu.cs.fluid.java.promise.IHasCustomBinding {
  syntax {     
    "module" $id:Info
  }
  
  public Operator getResultOp() { return Module.prototype; }
}
