# $Header$

/**
 * The leaf targets should all be TypeDeclPatterns,
 * and they are interpreted as the compilation units 
 * they are in
 */
public operator ScopedModule extends Module implements TargetedAnnotationInterface {
   syntax {
     $id:Info "for" targets:PromiseTarget
   }  
}