Tests for lock declarations.
The classes in this package are designed to test the "LocksOnce" rule that
lock names must be unique, and cannot be repeated along a class inheritance
branch.  The policy locks and the state locks share the same name space.
