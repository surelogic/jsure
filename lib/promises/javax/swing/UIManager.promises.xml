<?xml version="1.0" encoding="UTF-8"?>

<package name="javax.swing">
	<class name="UIManager">
		<Promise>@ThreadRole(AWT) for public **(**) &amp; !(add*Listener(*)
			| remove*Listener(*))</Promise>
		<Promise>@ThreadRoleTransparent for add*Listener(*) |
			remove*Listener(*)</Promise>

	<!--  Uncomment when nested classes work!
		<class name="LookAndFeelInfo">
			<Promise>@ThreadRole(AWT) for **(**)</Promise>
		</class>
	-->
	</class>
</package>
