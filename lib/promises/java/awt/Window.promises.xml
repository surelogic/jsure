<package name="java.awt">
  <class name="Window">

    <Promise>@ThreadRoleTransparent for add*Listener(**) | remove*Listener(**)</Promise>

    <Promise>@ThreadRole(AWT) for get*(**) | is*(**)</Promise>

    <Promise>@ThreadRole(AWT) for add*(**) &amp; !(add*Listener(**))</Promise>

    <Promise>@ThreadRole(AWT) for process*(**) | dispose()</Promise>

    <Promise>@ThreadRole(AWT) for set*(**)</Promise>

    <Promise>@ThreadRole(AWT) for toFront() | toBack()</Promise>

    <Promise>@ThreadRole(AWT) for pack() | show() | hide()</Promise>

  </class>
</package>
