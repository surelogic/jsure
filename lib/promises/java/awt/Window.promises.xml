<package name="java.awt">
  <class name="Window">

    <Promise>@ThreadRoleTransparent for add*Listener(**) | remove*Listener(**)</Promise>

    <Promise>@ThreadRole(GUI) for get*(**) | is*(**)</Promise>

    <Promise>@ThreadRole(GUI) for add*(**) &amp; !(add*Listener(**))</Promise>

    <Promise>@ThreadRole(GUI) for process*(**) | dispose()</Promise>

    <Promise>@ThreadRole(GUI) for set*(**)</Promise>

    <Promise>@ThreadRole(GUI) for toFront() | toBack()</Promise>

    <Promise>@ThreadRole(GUI) for pack() | show() | hide()</Promise>

  </class>
</package>
