<package name="java.awt.event">
  <Promise>@colorImport(java.awt.*) for *</Promise>

      <Promise>@Transparent for add*Listener(**) | remove*Listener(**)</Promise>

  <Promise>@Color(GUI) for get*(**)</Promise>

  <Promise>@Color(GUI) for add*(**) &amp; !(add*Listener(**))</Promise>

  <Promise>@Color(GUI) for *Changed(**)</Promise>

  <Promise>@Color(GUI) for new(**)</Promise>

  <Promise>@Color(GUI) for process*(**)</Promise>

  <Promise>@Color(GUI) for set*(**)</Promise>

  <Promise>@Color(GUI) for updateUI()</Promise>
  <Promise>@Transparent for revalidate()</Promise>

  <Promise>@Transparent for repaint(**)</Promise>

  <Promise>@Color(AWT) for fire*(**)</Promise>


  <Promise>@Color(AWT) for requestFocus*(**)</Promise>


  <Promise>@Color(AWT) for paint*(**)</Promise>

  <!--Promise>@Color(AWT) for public *Adapter:*()</Promise>
    
  <Promise>@Color(AWT) for public *Event:get*()</Promise>    
    
  <Promise>@Color(AWT) for public *Listener:*()</Promise>    
    
  <Promise>@Color(AWT) for public Key*er:key*()</Promise-->    
  
  <Promise>@Color(AWT) for public key*(java.awt.event.KeyEvent)</Promise>        

</package>
