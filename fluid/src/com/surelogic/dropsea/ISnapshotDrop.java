package com.surelogic.dropsea;

/**
 * A marker interface for drops that are intended to be output into the
 * snapshot.
 */
public interface ISnapshotDrop extends IDrop {
  // a marker interface.
}
