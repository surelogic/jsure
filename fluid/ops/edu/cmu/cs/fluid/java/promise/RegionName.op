# $Header: /cvs/fluid/fluid/ops/edu/cmu/cs/fluid/java/promise/RegionName.op,v 1.3 2006/07/03 14:47:51 chance Exp $

/** A use of a named region.  Binding uses standard scoping
 * rules to determine which region is indicated.
 */
public operator RegionName extends RegionSpecification {
  syntax { $id:Info }
  properties {
    bindsTo=Region
  }
}
